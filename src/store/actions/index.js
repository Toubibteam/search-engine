'use strict'

import fetchCodes from './fetchCodes'
import trackCodeSelection from './trackCodeSelection'

const actions = {
  'fetchCodes': fetchCodes,
  'trackCodeSelection': trackCodeSelection
}

export default actions
